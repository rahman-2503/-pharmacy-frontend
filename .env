# Backend API base URL (used in production builds)
# Update src/environments/environment.prod.ts with your deployed backend URL before building for production.
# Example: API_URL=https://your-api.com/api
# For local dev, requests are proxied via proxy.conf.json to http://localhost:8080.
API_URL=/api
